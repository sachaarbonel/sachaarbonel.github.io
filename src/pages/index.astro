---
import BaseLayout from '../layouts/BaseLayout.astro'
import { SocialLinks } from '../consts'
import { Icon } from 'astro-icon/components'
import BlogCard from '../components/BlogCard.astro'
import { getCollection } from 'astro:content'

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 4)
---

<BaseLayout>
	<main
		class="app-container flex flex-col gap-12 justify-center py-36 md:py-48 pt-44 md:pt-56 relative"
	>
		<div>
			<p class="block text-3xl opacity-70 mt-9 mb-4">Hi!, I'm</p>
			<h1 class="text-8xl font-bold leading-none">Sacha Arbonel</h1>
		</div>
		<p class="text-2xl max-w-2xl font-light leading-normal">
			I build speech AI and video infra.
		</p>
		<div class="flex flex-wrap gap-8">
			<a class="btn-secondary" href="/blog">Read writing</a>
			<a class="btn-primary" href="#selected-work">View projects</a>
			<a
				class="text-xl text-primary dark:text-primary-dark underline hover:underline underline-offset-4"
				href="mailto:sacha@arbonel.com">Email me</a
			>
		</div>
	</main>

	<!-- Recent Posts Section -->
	<section class="app-container">
		<h2 class="section-title">Recent writing</h2>
		<div class="posts-grid-container">
			{posts.map((post) => <BlogCard post={post} />)}
		</div>
		<a class="btn-secondary" href="/blog"
			>Read All
			<Icon size={20} name="mdi:arrow-right" />
		</a>
	</section>

	<!-- Intro Block -->
	<section class="app-container py-42 grid lg:grid-cols-2 gap-28 lg:gap-10">
		<div class="space-y-6">
			<h2 class="section-title">Intro</h2>
			<p class="text-lg max-w-xl pb-4">
				I work on ASR, WebRTC, and GPU runtimes.<br />
				I keep things simple and fast.
			</p>
		</div>
		<div class="space-y-6">
			<h2 class="section-title">Get in touch</h2>
			<p class="text-lg max-w-xl">
				Want to ship faster?<br />
				Email me.<br />
				I reply in 24 hours.
			</p>
			<div class="flex flex-wrap mt-auto space-x-4">
				<a class="btn-primary" href="mailto:sacha.arbonel@hotmail.fr">
					Email me
					<Icon height={20} name="mdi:email" />
				</a>
			</div>
		</div>
	</section>

	<!-- Selected Work -->
	<section id="selected-work" class="app-container py-12">
		<h2 class="section-title">Selected Work</h2>
		<div class="grid gap-8 md:grid-cols-2">
			<div class="p-6 border rounded-xl">
				<h3 class="text-2xl font-semibold">Whisper@Scale</h3>
				<p class="mt-2 text-lg">
					Serving Whisper to 30+ customers at Stream 2k reqs/min at peak. p90
					latency cut by 50%. 35-40x cheaper than OpenAI.
				</p>
			</div>
			<div class="p-6 border rounded-xl">
				<h3 class="text-2xl font-semibold">
					<a
						href="https://huggingface.co/datasets/sachaarbonel/whisper-hallucinations"
						target="_blank"
						rel="noopener noreferrer"
					>
						Whisper-Hallucinations (Noise)
					</a>
				</h3>
				<p class="mt-2 text-lg">Multilingual noise dataset for false-speech.</p>
			</div>
		</div>
	</section>

	<!-- Talks -->
	<section id="talks" class="app-container py-12">
		<h2 class="section-title">Talks</h2>
		<ul class="grid gap-8 md:grid-cols-2">
			<li class="p-6 border rounded-xl">
				<h3 class="text-2xl font-semibold">
					<a
						href="https://www.youtube.com/watch?v=MSEoOkg8oPY"
						target="_blank"
						rel="noopener noreferrer"
					>
						Reinventing Speech-to-Text Transcriptions with Go and Whisper
					</a>
				</h3>
				<p class="mt-2 text-lg">
					"You have to build the transcriptions feature as good as Google and
					Zoom but we don't have a similar budget". That was a challenge we
					faced and solved with hundreds of coffee cups and incredible hair
					loss, our solution is cost-effective, open-source, and fits within our
					Go stack. Whisper.cpp is a lightweight implementation of Open AI’s
					Whisper speech-to-text model, that comes with Go bindings. This
					solution is leveraged by a lot of emerging startups and existing
					companies.
				</p>
			</li>
		</ul>
	</section>
</BaseLayout>
